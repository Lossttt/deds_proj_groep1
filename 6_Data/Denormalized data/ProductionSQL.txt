//ProductionWorkOrder(production.ScrapReason, production.WorkOrder, Production.WorkorderRouting)

SQLcode voor het maken van de ProductionWorkOrder tabel:

CREATE TABLE Production.WorkOrder (
    ScrapReasonID INT PRIMARY KEY,           
    Name VARCHAR(255),                       
    ModifiedDate DATETIME,                   
    WorkOrderID INT,                         
    ProductID INT,                           
    OrderQty INT,                            
    StockedQty INT,                          
    ScrappedQty INT,                         
    StartDate DATETIME,                      
    EndDate DATETIME,                        
    DueDate DATETIME,                        
    OperationSequence INT,                   
    LocationID INT,                          
    ScheduledStartDate DATETIME,             
    ScheduledEndDate DATETIME,               
    ActualStartDate DATETIME,                
    ActualEndDate DATETIME,                  
    ActualResourceHrs FLOAT,                 
    PlannedCost DECIMAL(18, 2),              
    ActualCost DECIMAL(18, 2)                
);



//Production.product: (Production.Product, Production.TransactionHistory, Production.TransactionHistoryArchive, Production.ProductSubcategory, Production.ProductCategory, Production.ProductCostHistory, Production.ProductDocument, Production.ProductListPriceHistory, Production.ProductReview)

SQLcode voor het maken van de ProductionProduct tabel:
CREATE TABLE Production.Product (
    ProductID INT PRIMARY KEY,
    Name VARCHAR(255),
    ProductNumber VARCHAR(25),
    MakeFlag BIT,
    FinishedGoodsFlag BIT,
    Color VARCHAR(15),
    SafetyStockLevel SMALLINT,
    ReorderPoint SMALLINT,
    StandardCost DECIMAL(18, 2),
    ListPrice DECIMAL(18, 2),
    Size VARCHAR(5),
    SizeUnitMeasureCode NCHAR(3),
    WeightUnitMeasureCode NCHAR(3),
    Weight DECIMAL(8, 2),
    DaysToManufacture INT,
    ProductLine NCHAR(2),
    Class NCHAR(2),
    Style NCHAR(2),
    ProductSubcategoryID INT,
    ProductModelID INT,
    SellStartDate DATETIME,
    SellEndDate DATETIME,
    DiscontinuedDate DATETIME,
    rowguid UNIQUEIDENTIFIER,
    ModifiedDate DATETIME,
    TransactionID INT,
    ReferenceOrderID INT,
    ReferenceOrderLineID INT,
    TransactionDate DATETIME,
    TransactionType NCHAR(1),
    Quantity INT,
    ActualCost DECIMAL(18, 2),
    DocumentNode HIERARCHYID,
    DocumentLevel INT,
    Title VARCHAR(255),
    Owner VARCHAR(255),
    FolderFlag BIT,
    FileName VARCHAR(255),
    FileExtension VARCHAR(8),
    Revision NCHAR(5),
    ChangeNumber INT,
    Status NCHAR(1),
    DocumentSummary NVARCHAR(MAX),
    [Document] VARBINARY(MAX),
    ProductReviewID INT,
    ReviewerName NVARCHAR(50),
    ReviewDate DATETIME,
    EmailAddress NVARCHAR(50),
    Rating INT,
    Comments NVARCHAR(3850)
);




//Production.productPhoto: (Production.ProductPhoto, Production.ProductProductPhoto )
SQL code voor het maken van de ProductionPhoto tabel:
CREATE TABLE Production.ProductPhoto (
    ProductPhotoID INT PRIMARY KEY,
    ThumbNailPhoto VARBINARY(MAX),
    ThumbnailPhotoFileName VARCHAR(255),
    LargePhoto VARBINARY(MAX),
    LargePhotoFileName VARCHAR(255),
    ModifiedDate DATETIME
);




//Production.BillOfMaterials: (production.BillOfMaterials, Production.UnitMeasure)
SQL code voor het maken van de Production.BillOfMaterials tabel:
CREATE TABLE Production.BillOfMaterials (
    BillOfMaterialsID INT,
    ProductAssemblyID INT,
    ComponentID INT,
    StartDate DATETIME,
    EndDate DATETIME,
    UnitMeasureCode NCHAR(3) PRIMARY KEY,
    BOMLevel INT,
    PerAssemblyQty DECIMAL(18, 4),
    ModifiedDate DATETIME,
    Name VARCHAR(255)
);



//Production.ProductInventory: (Production.Inventory, Production.Location)
SQL code voor het maken van de Production.ProductInventory tabel:
CREATE TABLE Production.ProductInventory (
    ProductID INT,
    LocationID INT,
    Shelf VARCHAR(10),
    Bin INT,
    Quantity INT,
    rowguid UNIQUEIDENTIFIER,
    ModifiedDate DATETIME,
    Name VARCHAR(255),
    CostRate DECIMAL(18, 2),
    Availability INT,
    PRIMARY KEY (ProductID, LocationID)
);




//Production.productModel(Production.ProductModel, production.ProductModelIllustration, production.ProductModelProductDescriptionCulture )
SQL code voor het maken van de Production.ProductModel tabel:
CREATE TABLE Production.ProductModel (
    ProductModelID INT PRIMARY KEY,
    Name VARCHAR(255),
    CatalogDescription NVARCHAR(MAX),
    Instructions NVARCHAR(MAX),
    rowguid UNIQUEIDENTIFIER,
    ModifiedDate DATETIME,
    IllustrationID INT,
    ProductDescriptionID INT,
    CultureID NCHAR(6)
);


